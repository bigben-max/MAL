#include "lidar_mapping/io/data_io.h"

namespace base::ldm {

bool loadKittiCloud(const std::string &file_name,
                    base::LidarMeasurement *lidar_mes) {
                      
  return true;
}

} // namespace base::ldm
